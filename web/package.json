{
  "name": "@daomar/agentfleet-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && cp -r static-root/* static-root/.well-known dist/",
    "preview": "vite preview",
    "test": "vitest run",
    "verify:static-pages": "node scripts/verify-static-pages.mjs"
  },
  "dependencies": {
    "@azure/msal-browser": "^4.12.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "jsdom": "^26.1.0",
    "typescript": "^5.8.0",
    "vite": "^6.3.0",
    "vitest": "^3.1.0"
  }
}
